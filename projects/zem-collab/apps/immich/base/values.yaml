immich:
  persistence:
    library:
      existingClaim: photos
env:
  REDIS_HOSTNAME: redis-master
  REDIS_PASSWORD: 
    valueFrom:
      secretKeyRef:
        name: redis
        key: redis-password
  DB_HOSTNAME: 
    valueFrom:
      secretKeyRef:
        name: cluster1-pguser-immich-user 
        key: pgbouncer-host
  DB_USERNAME:
    valueFrom:
      secretKeyRef:
        name: cluster1-pguser-immich-user 
        key: user
  DB_PASSWORD:
    valueFrom:
      secretKeyRef:
        name: cluster1-pguser-immich-user 
        key: password
  
  DB_DATABASE_NAME: immich
