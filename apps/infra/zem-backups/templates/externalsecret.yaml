apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: backup-credentials
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: oci-vault
    kind: SecretStore
  target:
    name: backup-credentials
  dataFrom:
    - extract:
        key: {{ .Values.b2.vaultSecretName }}
