{{ if .Values.features.openebs.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: externalsecrets
  namespace: argocd
spec:
  project: infra-externalsecrets
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
  source:
    repoURL: https://github.com/danfoster/zem-gitops
    targetRevision: main
    path: apps/infra/zem-externalsecrets
    helm:
      valuesObject:
      {{- toYaml .Values.features.externalsecrets | nindent 8 }}
  destination:
    server: "https://kubernetes.default.svc"
    namespace: external-secrets
{{ end }}