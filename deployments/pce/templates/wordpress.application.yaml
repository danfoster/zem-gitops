apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pce-wordpress-{{ .Values.env }}
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: pce-{{ .Values.env }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
  source:
    repoURL: https://github.com/danfoster/zem-gitops
    targetRevision: main
    path: apps/zem-external/wordpress
    helm:
      releaseName: pce-wordpress
      valuesObject:
      {{- toYaml .Values.wordpress | nindent 8 }}
  destination:
    server: "https://kubernetes.default.svc"
    namespace: pce-{{ .Values.env }}
  ignoreDifferences:
    - group: argoproj.io
      kind: Application
      jsonPointers:
      - /spec/syncPolicy